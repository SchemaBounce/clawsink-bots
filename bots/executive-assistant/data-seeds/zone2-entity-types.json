{
  "entityTypes": [
    {
      "name": "ea_findings",
      "displayName": "Executive Findings",
      "description": "Executive briefings and cross-domain synthesis",
      "domain": "operations",
      "sampleFields": ["severity", "briefing_type", "summary", "action_items", "domain_summaries"],
      "fields": {
        "severity": {"type": "string", "required": true, "enum": ["low", "medium", "high", "critical"], "description": "Finding severity"},
        "briefing_type": {"type": "string", "required": true, "description": "Type of executive briefing"},
        "summary": {"type": "string", "required": true, "description": "Executive summary"},
        "action_items": {"type": "array", "description": "Action items for leadership"},
        "domain_summaries": {"type": "object", "description": "Per-domain summary data"}
      }
    },
    {
      "name": "ea_alerts",
      "displayName": "Executive Alerts",
      "description": "Executive-level alerts requiring human attention",
      "domain": "operations",
      "sampleFields": ["severity", "title", "description", "action_required", "source_bot"],
      "fields": {
        "severity": {"type": "string", "required": true, "enum": ["low", "medium", "high", "critical"], "description": "Alert severity"},
        "title": {"type": "string", "required": true, "description": "Alert title"},
        "description": {"type": "string", "required": true, "description": "Alert details"},
        "action_required": {"type": "string", "description": "Required executive action"},
        "source_bot": {"type": "string", "description": "Bot that raised the alert"}
      }
    },
    {
      "name": "tasks",
      "displayName": "Tasks",
      "description": "Cross-domain action items and follow-ups",
      "domain": "operations",
      "sampleFields": ["title", "assigned_to", "due_date", "status", "priority", "source"],
      "fields": {
        "title": {"type": "string", "required": true, "description": "Task title"},
        "assigned_to": {"type": "string", "description": "Assignee"},
        "due_date": {"type": "date", "description": "Due date"},
        "status": {"type": "string", "enum": ["pending", "in_progress", "completed", "cancelled"], "description": "Task status"},
        "priority": {"type": "string", "enum": ["low", "medium", "high", "urgent"], "description": "Priority level"},
        "source": {"type": "string", "description": "Source bot or domain"}
      }
    }
  ]
}

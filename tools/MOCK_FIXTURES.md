# Mock catalog fixtures (`mock-*`)

The `tools/mock-*` entries are platform test fixtures, not customer
integrations. They exercise the hosting path end to end and are hidden from
the marketplace catalog.
